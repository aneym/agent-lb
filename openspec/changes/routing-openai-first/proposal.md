## Why
The installed router favors scarce Anthropic implementation and admits blocked or stale pools.

## What Changes
- Track the installed CLI, table and hooks; install only from checkout sources.
- Prefer OpenAI builders and Astra validation, with a required Opus money-path read.
- Gate picks on fresh per-account short and weekly reserves; expose usable-now counts.
- Add a batched typed Jev classifier and auditable dispatch headers.

## Impact
Local coding-agent configuration only. No server, credentials, live installation or deployment changes.
