# Verification

Weekly-only ascending and descending regression checks, including conflicting five-hour values and absent weekly telemetry, passed in the 204-test Swift suite (exit 0). Release build, strict signature verification, change/spec validation and diff check passed (exit 0).

Book is running the matching bundle (SHA-256 `dc5ce98d542c9d136ff006b518cf2eb5021e737fbeccf6f5f20afbb3917d4c1e`). Native startup diagnostics show 12 accounts, summary loaded, no section errors and an on-screen status item. Sort behavior is local fixture proof; no new provider inference or live popover interaction was performed.
