# Tasks
- [x] Extract the per-class eligibility loop from `route pick` into `class_candidates`
- [x] Add `route menu` with pool facts and 5-hour windows from `/api/accounts`
- [x] Add `jev pick` (jev repo) with keel's abstain thresholds
- [x] Rewrite `open-factory` on `route` + `jev pick`; remove catalog and per-project ledgers
- [x] Tests: `route menu` vs `route pick`; `open-factory route` host re-check and fallbacks
