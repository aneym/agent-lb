# Tasks

## 1. Fail-closed policy hook

- [x] 1.1 Register the managed `seat-guard` command with a shell denial fallback and verify its generated settings value.
- [x] 1.2 Make the guard emergency printer independent of `json` and verify a shadowed `json` import emits denial output.

## 2. Verification

- [x] 2.1 Add and run regressions for the shadowed module and failing installed command with `/usr/bin/python3`.
- [x] 2.2 Validate the OpenSpec change and the affected test suites.
