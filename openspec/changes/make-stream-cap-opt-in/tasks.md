- [x] Change the default account stream cap to unlimited (`0`) while preserving
  explicit nonzero cap behavior.
- [x] Update operator-facing env example comments for response-create and stream
  account caps.
- [x] Update focused unit tests so stream-cap rejection tests opt into a nonzero
  configured cap.
- [x] Validate the OpenSpec change and focused tests.
