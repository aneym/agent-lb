# Route until a live quota failure

Usage snapshots are advisory. Stale primary and weekly percentages currently deny both providers before a real request, including accounts that have just served successful pinned probes. Preserve hard account, authentication, model-compatibility, and pin guards while making the upstream response authoritative for quota rejection.
