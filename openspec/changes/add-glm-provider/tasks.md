# Tasks

- [x] Add GLM provider registration and encrypted API-key account import.
- [x] Route `glm-*` Messages requests through GLM-only account selection and the Z.AI Anthropic-compatible endpoint.
- [x] Keep GLM quota keys, request logs, and sticky-session keys provider-scoped.
- [x] Add focused backend coverage for provider registry, account import, GLM routing, and Anthropic compatibility.
- [x] Wire the local `glm` zsh command through agent-lb.
- [x] Import a usable local GLM account and verify a live `/v1/messages` request through agent-lb.
- [x] Run OpenSpec validation and update this checklist with final proof.
