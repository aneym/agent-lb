# Open Factory session addendum

This Claude Code session was started by `open-factory start`.

- Factory state: `.open-factory/`
- Orchestrator rules: `.open-factory/ORCHESTRATOR.md`
- Seats runnable now: `open-factory seats` (the `route menu`)
- Per-call routing: `open-factory route --class <class> "<task>"`
