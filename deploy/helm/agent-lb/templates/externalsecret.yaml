{{- if .Values.externalSecrets.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "agent-lb.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "agent-lb.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStoreRef.name }}
    kind: {{ .Values.externalSecrets.secretStoreRef.kind }}
  target:
    name: {{ include "agent-lb.secretName" . }}
    creationPolicy: Owner
  data:
    - secretKey: {{ .Values.auth.secretKeys.databaseUrl }}
      remoteRef:
        key: {{ include "agent-lb.fullname" . }}
        property: database-url
    - secretKey: {{ .Values.auth.secretKeys.encryptionKey }}
      remoteRef:
        key: {{ include "agent-lb.fullname" . }}
        property: encryption-key
{{- end }}
